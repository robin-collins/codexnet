# T604 installation and operator walkthrough evidence

Date: 2026-07-15 (Australia/Adelaide)

## Clean staged environment

The documented critical path was replayed in a new temporary root using only repository runbooks,
tracked source, synthetic fixtures, and local commands. No live interface, collector target,
appliance systemd state, Scanopy component, cron entry, protected nmap script, or external platform
was contacted or modified.

The walkthrough:

1. exported the candidate tracked source into a clean `/opt/field-discovery`-shaped directory;
2. created a new virtual environment and installed the exact `requirements-dev.lock` successfully;
3. copied and path-adjusted the sanitized example configuration, created a mode `0600` empty
   provider file, and passed `config validate`;
4. initialized/migrated a new temporary SQLite database and passed integrity/foreign-key checks;
5. imported one completed sanitized nmap fixture with `1 imported, 0 skipped, 0 deferred,
   0 errors`;
6. generated the stable DOCX/JSON pair and passed `report validate` before upload-ready output;
7. created a verified SQLite backup, restored it to a new path, and completed prune preview;
8. staged all six systemd units plus the sysusers declaration through the three documented
   installers without enabling or starting anything; and
9. ran the documented service removal script and proved all staged CodexNet units were removed.

The isolated walkthrough ended with `T604 walkthrough PASS`. The companion automated documentation
test also verifies local links, required workflow coverage, absence of common unsafe advice, unit
modes/names, and byte-preservation of synthetic protected nmap, cron, result-tree, and Scanopy
sentinels across staged removal.

## Deliberately manual checks

Starting hardened system services, packet capture, approved live collector reads, current Word
opening, and attachment to IT Glue, Datto RMM, or Autotask require the actual authorised appliance,
customer change window, and operator platform account. The runbooks make each explicit and do not
substitute a fixture result for those release/site checks. They require a second validation and
checksum after any DOCX change and prohibit framework-controlled upload.
